rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Users can only read and write their own user document
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Spaces can be read by anyone.
    // Any logged-in user can create a space.
    // Only the creator of the space can update or delete it.
    match /spaces/{spaceId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && request.auth.uid == resource.data.createdBy;
    }

    // Users can only manage their own favorites.
    // FAVORITES: Allow read for everyone to count likes.
    match /favorites/{favoriteId} {
      allow read: if true; // CHANGED: Allow read for everyone to count likes
      allow delete: if request.auth != null && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.userId;
    }
  }
}
